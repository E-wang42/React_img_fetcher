import styled from "styled-components";

const PageCentered = styled.main`
  margin: 0 auto;
  background: linear-gradient(to right, #fcb045, #fd1d1d, #833ab4);
  height: 100vh;
`;

export default PageCentered;
